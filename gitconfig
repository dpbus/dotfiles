[user]
  name = David Busse
  email = david@busse.net
[color]
  ui = auto
[alias]
  ci = commit
  co = checkout
  st = status
  staged = diff --cached
  unstaged = diff
  permission-reset = !git diff -p -R | grep -E \"^(diff|(old|new) mode)\" | git apply
[push]
  default = current
[core]
  editor = vim
  excludesfile = ~/.gitignore
[url "ssh://git@github.com/"]
  insteadOf = https://github.com/
