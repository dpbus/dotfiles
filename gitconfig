[user]
  name = David Busse
  email = dpb@essub.com
[color]
  ui = auto
[alias]
  ci = commit
  co = checkout
  st = status
  staged = diff --cached
  unstaged = diff
  permission-reset = !git diff -p -R | grep -E \"^(diff|(old|new) mode)\" | git apply
[push]
  default = current
[core]
  autocrlf = input
  editor = vim
  excludesfile = ~/.gitignore
